name: frontend
on:
    push:
      branches:
        - master
      paths:
        - 'dist/stuvatar/**'
    workflow_dispatch:
jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - run: |
            cd /scripts
            ./afterpull-Frontend
